{
  "name": "talent-matchmaker-backend",
  "version": "1.0.0",
  "description": "Backend for Talent Matchmaker Lite",
  "main": "app.js",
  "scripts": {
    "build": "cd ../frontend && npm install && npm run build",
    "start": "node app.js"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
